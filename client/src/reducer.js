export function reducer(state = {}, action) {
    // if (action.type === "SHOW_WANNABES") {
    //     state = {
    //         ...state,
    //         users: action.friendsList,
    //     };
    // }

    return state;
}
